#include <bits/stdc++.h>
using namespace std;

int main() {
    string input;
    vector<vector<int>> matrix;
    vector<int> buffer;
    int num;
    while(!EOF){
        cin>>num;
        buffer.push_back(num);
    }
    return 0;
}